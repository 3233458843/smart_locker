【续聊上下文】
目标：完成智能储物柜业务闭环（存件录入→绑定柜号/密码→取件开柜）的稳定联调。
约束：ESP32-S3 + LVGL + XST 实机环境；尽量沿用现有 UI 生成代码；优先真实业务闭环而非页面美化。
进度：截至 2026-04-21，已新增 locker NVS 映射库（user_id/locker_id/password/timestamp）；save_page 进入后会异步录入掌静脉并分配空柜、生成4位密码并落库；take_page 进入后会异步识别并自动开柜，同时保留密码取件路径；main 已调用 locker_db_init() 启动恢复数据。
待办/卡点：自动取件仍用 user_id%4 开柜，和 NVS 绑定模型不一致；LVGL 标签在后台任务中直接更新存在线程安全风险；Setting 页 READ/VERI/DELL 与状态区展示仍未完全打通；管理员/取件密码策略仍偏硬编码。
关键资料：核心文件 main/main.c、bsp/ui/generated/events_init.c、bsp/locker/locker.c、bsp/locker/locker.h；历史基线见 README.md（2026-04-17）与本地未提交 diff。
下次直接执行：先把 take 流程改为“识别 user_id→locker_db_get_entry_by_user→开对应柜并清理条目”，再把跨线程 UI 更新切到 LVGL 安全上下文，最后补齐 Setting 页命令与状态联动。
