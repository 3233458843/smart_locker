【续聊上下文】
目标：补齐 smart_locker 存/取件业务闭环，并让主页柜号状态按“是否已注册/已存件”动态显示。
约束：ESP32-S3 + ESP-IDF + LVGL + XST 实机项目；尽量沿用 generated UI；LVGL 控件只能在 LVGL 任务上下文更新；lockers[0..3] 对应 locker1..locker4。
进度：已并入 20260426_205520_2c7460 会话上下文；存件链路已包含 XST 录入、4位数字密码、locker_db_add_entry/NVS；取件密码链路已从硬编码改为 locker_db_get_entry_by_password；xst_exec_cmd 已能忽略 ENROLL_PROGRESS 等非目标回复继续等待；本轮删除了按 is_locked 更新主页灯的逻辑，改为 lvgl_demo_task 每 1s 用 has_item_in_locker(&lockers[i])/have_saved 更新 main_locker1..4 颜色（已存红、空闲绿）。
待办/卡点：需上板确认主页颜色与 NVS 恢复后 have_saved 一致；继续接通 save_page_label/bar 的业务状态反馈与蜂鸣提示；关注取件后 locker_db_remove_entry_by_locker 是否及时让主页变绿。
关键资料：核心文件 main/main.c、serve/serve.c、serve/serve.h、bsp/locker/locker.c、bsp/locker/locker.h、bsp/ui/generated/events_init.c、bsp/XST/xst.c；本地续聊 skill 位于 E:/esp_code/smart_locker/.skill/note-context-handoff。
下次直接执行：上板测试一次“存件→主页灯变红→密码/掌静脉取件→主页灯变绿”，若通过再做 save_page UI 进度与蜂鸣反馈。
